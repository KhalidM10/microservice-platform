apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: microservice-platform
spec:
  selector:
    app: document-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
